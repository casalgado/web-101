// En javascript hay unos elementos basicos con los que se construye.

// Strings:
// Texto. Se declaran usando comillas.

let announcement = "Hello World";

// Integers:
// Numeros. Se declaran utilizando numeros.
// (Los integers son sin decimales, para usar decimales es otro elemento llamado Float)

let age = 36;

// Booleans:
// Verdadero o Falso. Este tipo de objetos se usan en condicionales logicos.

let alive = true;
let dead = false;

// Variables:
// Se utilizan para almacenar otros elementos.
// Se declarar utilizando "let" o "const"
// las hemos estado utilizando arriba.

// console.log()
// el console.log() se utiliza para escribir valores en la consola cuando se corre un programa.
// normalmente se utiliza para debug.
